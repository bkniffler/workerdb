language: node_js
node_js:
  - '8'

sudo: false
cache:
  directories:
    - 'node_modules'

install:
  - yarn
  - cd packages/workerdb && yarn link
  - cd packages/react-workerdb && yarn link workerdb

script:
  - yarn build
  - yarn test
  - yarn coverage
